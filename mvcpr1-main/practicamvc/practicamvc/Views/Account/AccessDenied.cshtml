@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="container" style="max-width: 600px; margin-top: 100px; text-align: center;">
    <h2 style="color: var(--color-error);"> Acceso denegado</h2>
    <p style="margin-top: 16px; font-size: var(--font-size-lg);">
        No tienes permisos para acceder a esta pagina.
    </p>
    <a asp-controller="Home" asp-action="Index" class="btn btn--primary" style="margin-top: 24px;">
        Volver al inicio
    </a>


</div>
